import { APP_ROUTES } from '@common/constants/routes';

export const NARROW_MODEL_PROGRESS_ROUTES = new Set<string>([
  APP_ROUTES.analysisHistory,
  APP_ROUTES.coverLetter,
  APP_ROUTES.privacy,
  APP_ROUTES.vacancyComparison,
]);
